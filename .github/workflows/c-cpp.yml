name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: cd mylib/ ;
           make all ;
           cat mylib.so ;
           cd .. ; 
      #run: ./configure
    - name: make clean
      run: make clean
    - name: make build
      run: export LD_LIBRARY_PATH=/home/milostiv/programming/mylib:$LD_LIBRARY_PATH ;
           make all ;
    #- name: make check
      #run: make check
    #- name: make distcheck
      #run: make distcheck
